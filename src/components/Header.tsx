import React from 'react';

const Header: React.FC = () => {
  return (
    <>
      <h1>Training Data Viewer</h1>
      <p>Supports both JSON arrays and JSONL (line-delimited JSON) files</p>
    </>
  );
};

export default Header;
